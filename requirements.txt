# LLM 调用（OpenAI 兼容模式对接百炼）
openai>=1.40.0

# RAG 向量检索（轻量实现，仅用 numpy 算余弦相似度）
numpy>=1.26.0

# 混合检索：BM25 关键词路 + 中文分词；Rerank 调用用 requests（自带证书）
rank-bm25>=0.2.2
jieba>=0.42.1
requests>=2.31.0

# 二进制文档提取：PDF / Word（纯 Python，无系统依赖）
pypdf>=4.0.0
python-docx>=1.1.0

# 调用链追踪（未配置凭证时自动降级本地 JSONL，不影响运行）
langfuse>=4.0.0

# MCP 官方 Python SDK（钉住 1.x：2.0 移除了 FastMCP，接口不兼容）
mcp>=1.2.0,<2.0.0

# Web 界面（5.0+ 默认 messages 对话格式）
gradio>=5.0.0

# 环境变量
python-dotenv>=1.0.0
