import {
  genStyleHooks,
  use_x_component_config_default,
  use_x_provider_context_default
} from "./chunk-EQJ3GEUA.js";
import {
  ArrowUpOutlined_default,
  AudioMutedOutlined_default,
  AudioOutlined_default,
  ClearOutlined_default
} from "./chunk-VVQXRWPN.js";
import {
  button_default,
  es_default,
  flex_default,
  get,
  input_default,
  merge,
  pickAttrs,
  unit,
  useEvent,
  useMergedState
} from "./chunk-NDN4O64D.js";
import "./chunk-THBX4FV6.js";
import {
  CloseOutlined_default,
  _extends,
  require_classnames
} from "./chunk-5UNR746G.js";
import "./chunk-NUMECXU6.js";
import {
  require_react
} from "./chunk-RLJ2RCJQ.js";
import {
  __toESM
} from "./chunk-DC5AMYBS.js";

// node_modules/@ant-design/x/es/sender/index.js
var import_classnames4 = __toESM(require_classnames());
var import_react5 = __toESM(require_react());

// node_modules/@ant-design/x/es/_util/hooks/use-proxy-imperative-handle.js
var import_react = __toESM(require_react());
function useProxyImperativeHandle(ref, init) {
  return (0, import_react.useImperativeHandle)(ref, () => {
    const refObj = init();
    const {
      nativeElement
    } = refObj;
    return new Proxy(nativeElement, {
      get(obj, prop) {
        if (refObj[prop]) {
          return refObj[prop];
        }
        return Reflect.get(obj, prop);
      }
    });
  });
}

// node_modules/@ant-design/x/es/sender/SenderHeader.js
var import_classnames = __toESM(require_classnames());
var React = __toESM(require_react());
var SendHeaderContext = React.createContext({});
var collapseHeight = () => ({
  height: 0
});
var expandedHeight = (ele) => ({
  height: ele.scrollHeight
});
function SenderHeader(props) {
  const {
    title,
    onOpenChange,
    open,
    children,
    className,
    style,
    classNames: classes = {},
    styles = {},
    closable,
    forceRender
  } = props;
  const {
    prefixCls
  } = React.useContext(SendHeaderContext);
  const headerCls = `${prefixCls}-header`;
  return React.createElement(es_default, {
    motionEnter: true,
    motionLeave: true,
    motionName: `${headerCls}-motion`,
    leavedClassName: `${headerCls}-motion-hidden`,
    onEnterStart: collapseHeight,
    onEnterActive: expandedHeight,
    onLeaveStart: expandedHeight,
    onLeaveActive: collapseHeight,
    visible: open,
    forceRender
  }, ({
    className: motionClassName,
    style: motionStyle
  }) => {
    return React.createElement("div", {
      className: (0, import_classnames.default)(headerCls, motionClassName, className),
      style: {
        ...motionStyle,
        ...style
      }
    }, (closable !== false || title) && React.createElement("div", {
      className: (
        // We follow antd naming standard here.
        // So the header part is use `-header` suffix.
        // Though its little bit weird for double `-header`.
        (0, import_classnames.default)(`${headerCls}-header`, classes.header)
      ),
      style: {
        ...styles.header
      }
    }, React.createElement("div", {
      className: `${headerCls}-title`
    }, title), closable !== false && React.createElement("div", {
      className: `${headerCls}-close`
    }, React.createElement(button_default, {
      type: "text",
      icon: React.createElement(CloseOutlined_default, null),
      size: "small",
      onClick: () => {
        onOpenChange == null ? void 0 : onOpenChange(!open);
      }
    }))), children && React.createElement("div", {
      className: (0, import_classnames.default)(`${headerCls}-content`, classes.content),
      style: {
        ...styles.content
      }
    }, children));
  });
}

// node_modules/@ant-design/x/es/sender/components/ActionButton.js
var import_classnames2 = __toESM(require_classnames());
var React2 = __toESM(require_react());
var ActionButtonContext = React2.createContext(null);
function ActionButton(props, ref) {
  const {
    className,
    action,
    onClick,
    ...restProps
  } = props;
  const context = React2.useContext(ActionButtonContext);
  const {
    prefixCls,
    disabled: rootDisabled
  } = context;
  const mergedDisabled = restProps.disabled ?? rootDisabled ?? context[`${action}Disabled`];
  return React2.createElement(button_default, _extends({
    type: "text"
  }, restProps, {
    ref,
    onClick: (e) => {
      var _a;
      if (mergedDisabled) {
        return;
      }
      (_a = context[action]) == null ? void 0 : _a.call(context);
      onClick == null ? void 0 : onClick(e);
    },
    className: (0, import_classnames2.default)(prefixCls, className, {
      [`${prefixCls}-disabled`]: mergedDisabled
    })
  }));
}
var ActionButton_default = React2.forwardRef(ActionButton);

// node_modules/@ant-design/x/es/sender/components/ClearButton.js
var React3 = __toESM(require_react());
function ClearButton(props, ref) {
  return React3.createElement(ActionButton_default, _extends({
    icon: React3.createElement(ClearOutlined_default, null)
  }, props, {
    action: "onClear",
    ref
  }));
}
var ClearButton_default = React3.forwardRef(ClearButton);

// node_modules/@ant-design/x/es/sender/components/LoadingButton.js
var import_classnames3 = __toESM(require_classnames());
var React5 = __toESM(require_react());

// node_modules/@ant-design/x/es/sender/StopLoading.js
var import_react2 = __toESM(require_react());
var StopLoadingIcon = (0, import_react2.memo)((props) => {
  const {
    className
  } = props;
  return import_react2.default.createElement("svg", {
    color: "currentColor",
    viewBox: "0 0 1000 1000",
    xmlns: "http://www.w3.org/2000/svg",
    className
  }, import_react2.default.createElement("title", null, "Stop Loading"), import_react2.default.createElement("rect", {
    fill: "currentColor",
    height: "250",
    rx: "24",
    ry: "24",
    width: "250",
    x: "375",
    y: "375"
  }), import_react2.default.createElement("circle", {
    cx: "500",
    cy: "500",
    fill: "none",
    r: "450",
    stroke: "currentColor",
    strokeWidth: "100",
    opacity: "0.45"
  }), import_react2.default.createElement("circle", {
    cx: "500",
    cy: "500",
    fill: "none",
    r: "450",
    stroke: "currentColor",
    strokeWidth: "100",
    strokeDasharray: "600 9999999"
  }, import_react2.default.createElement("animateTransform", {
    attributeName: "transform",
    dur: "1s",
    from: "0 500 500",
    repeatCount: "indefinite",
    to: "360 500 500",
    type: "rotate"
  })));
});
var StopLoading_default = StopLoadingIcon;

// node_modules/@ant-design/x/es/sender/components/LoadingButton.js
function LoadingButton(props, ref) {
  const {
    prefixCls
  } = React5.useContext(ActionButtonContext);
  const {
    className
  } = props;
  return React5.createElement(ActionButton_default, _extends({
    icon: React5.createElement(StopLoading_default, {
      className: `${prefixCls}-loading-icon`
    }),
    color: "primary",
    variant: "text",
    shape: "circle"
  }, props, {
    className: (0, import_classnames3.default)(className, `${prefixCls}-loading-button`),
    action: "onCancel",
    ref
  }));
}
var LoadingButton_default = React5.forwardRef(LoadingButton);

// node_modules/@ant-design/x/es/sender/components/SendButton.js
var React6 = __toESM(require_react());
function SendButton(props, ref) {
  return React6.createElement(ActionButton_default, _extends({
    icon: React6.createElement(ArrowUpOutlined_default, null),
    type: "primary",
    shape: "circle"
  }, props, {
    action: "onSend",
    ref
  }));
}
var SendButton_default = React6.forwardRef(SendButton);

// node_modules/@ant-design/x/es/sender/components/SpeechButton/index.js
var React8 = __toESM(require_react());

// node_modules/@ant-design/x/es/sender/components/SpeechButton/RecordingIcon.js
var import_react3 = __toESM(require_react());
var SIZE = 1e3;
var COUNT = 4;
var RECT_WIDTH = 140;
var RECT_RADIUS = RECT_WIDTH / 2;
var RECT_HEIGHT_MIN = 250;
var RECT_HEIGHT_MAX = 500;
var DURATION = 0.8;
function RecordingIcon({
  className
}) {
  return import_react3.default.createElement("svg", {
    color: "currentColor",
    viewBox: `0 0 ${SIZE} ${SIZE}`,
    xmlns: "http://www.w3.org/2000/svg",
    className
  }, import_react3.default.createElement("title", null, "Speech Recording"), Array.from({
    length: COUNT
  }).map((_, index) => {
    const dest = (SIZE - RECT_WIDTH * COUNT) / (COUNT - 1);
    const x = index * (dest + RECT_WIDTH);
    const yMin = SIZE / 2 - RECT_HEIGHT_MIN / 2;
    const yMax = SIZE / 2 - RECT_HEIGHT_MAX / 2;
    return import_react3.default.createElement("rect", {
      fill: "currentColor",
      rx: RECT_RADIUS,
      ry: RECT_RADIUS,
      height: RECT_HEIGHT_MIN,
      width: RECT_WIDTH,
      x,
      y: yMin,
      key: index
    }, import_react3.default.createElement("animate", {
      attributeName: "height",
      values: `${RECT_HEIGHT_MIN}; ${RECT_HEIGHT_MAX}; ${RECT_HEIGHT_MIN}`,
      keyTimes: "0; 0.5; 1",
      dur: `${DURATION}s`,
      begin: `${DURATION / COUNT * index}s`,
      repeatCount: "indefinite"
    }), import_react3.default.createElement("animate", {
      attributeName: "y",
      values: `${yMin}; ${yMax}; ${yMin}`,
      keyTimes: "0; 0.5; 1",
      dur: `${DURATION}s`,
      begin: `${DURATION / COUNT * index}s`,
      repeatCount: "indefinite"
    }));
  }));
}

// node_modules/@ant-design/x/es/sender/components/SpeechButton/index.js
function SpeechButton(props, ref) {
  const {
    speechRecording,
    onSpeechDisabled,
    prefixCls
  } = React8.useContext(ActionButtonContext);
  let icon = null;
  if (speechRecording) {
    icon = React8.createElement(RecordingIcon, {
      className: `${prefixCls}-recording-icon`
    });
  } else if (onSpeechDisabled) {
    icon = React8.createElement(AudioMutedOutlined_default, null);
  } else {
    icon = React8.createElement(AudioOutlined_default, null);
  }
  return React8.createElement(ActionButton_default, _extends({
    icon,
    color: "primary",
    variant: "text"
  }, props, {
    action: "onSpeech",
    ref
  }));
}
var SpeechButton_default = React8.forwardRef(SpeechButton);

// node_modules/@ant-design/x/es/sender/style/header.js
var genSenderHeaderStyle = (token) => {
  const {
    componentCls,
    calc
  } = token;
  const headerCls = `${componentCls}-header`;
  return {
    [componentCls]: {
      [headerCls]: {
        borderBottomWidth: token.lineWidth,
        borderBottomStyle: "solid",
        borderBottomColor: token.colorBorder,
        // ======================== Header ========================
        "&-header": {
          background: token.colorFillAlter,
          fontSize: token.fontSize,
          lineHeight: token.lineHeight,
          paddingBlock: calc(token.paddingSM).sub(token.lineWidthBold).equal(),
          paddingInlineStart: token.padding,
          paddingInlineEnd: token.paddingXS,
          display: "flex",
          borderRadius: {
            _skip_check_: true,
            value: calc(token.borderRadius).mul(2).equal()
          },
          borderEndStartRadius: 0,
          borderEndEndRadius: 0,
          [`${headerCls}-title`]: {
            flex: "auto"
          }
        },
        // ======================= Content ========================
        "&-content": {
          padding: token.padding
        },
        // ======================== Motion ========================
        "&-motion": {
          transition: ["height", "border"].map((prop) => `${prop} ${token.motionDurationSlow}`).join(","),
          overflow: "hidden",
          "&-enter-start, &-leave-active": {
            borderBottomColor: "transparent"
          },
          "&-hidden": {
            display: "none"
          }
        }
      }
    }
  };
};
var header_default = genSenderHeaderStyle;

// node_modules/@ant-design/x/es/sender/style/index.js
var genSenderStyle = (token) => {
  const {
    componentCls,
    padding,
    paddingSM,
    paddingXS,
    paddingXXS,
    lineWidth,
    lineWidthBold,
    calc
  } = token;
  return {
    [componentCls]: {
      position: "relative",
      width: "100%",
      boxSizing: "border-box",
      boxShadow: `${token.boxShadowTertiary}`,
      transition: `background ${token.motionDurationSlow}`,
      // Border
      borderRadius: {
        _skip_check_: true,
        value: calc(token.borderRadius).mul(2).equal()
      },
      borderColor: token.colorBorder,
      borderWidth: 0,
      borderStyle: "solid",
      // Border
      "&:after": {
        content: '""',
        position: "absolute",
        inset: 0,
        pointerEvents: "none",
        transition: `border-color ${token.motionDurationSlow}`,
        borderRadius: {
          _skip_check_: true,
          value: "inherit"
        },
        borderStyle: "inherit",
        borderColor: "inherit",
        borderWidth: lineWidth
      },
      // Focus
      "&:focus-within": {
        boxShadow: `${token.boxShadowSecondary}`,
        borderColor: token.colorPrimary,
        "&:after": {
          borderWidth: lineWidthBold
        }
      },
      "&-disabled": {
        background: token.colorBgContainerDisabled
      },
      // ============================== RTL ==============================
      [`&${componentCls}-rtl`]: {
        direction: "rtl"
      },
      // ============================ Content ============================
      [`${componentCls}-content`]: {
        display: "flex",
        gap: paddingXS,
        width: "100%",
        paddingBlock: paddingSM,
        paddingInlineStart: padding,
        paddingInlineEnd: paddingSM,
        boxSizing: "border-box",
        alignItems: "flex-end"
      },
      // ============================ Prefix =============================
      [`${componentCls}-prefix`]: {
        flex: "none"
      },
      // ============================= Input =============================
      [`${componentCls}-input`]: {
        padding: 0,
        borderRadius: 0,
        flex: "auto",
        alignSelf: "center",
        minHeight: "auto"
      },
      // ============================ Actions ============================
      [`${componentCls}-actions-list`]: {
        flex: "none",
        display: "flex",
        "&-presets": {
          gap: token.paddingXS
        }
      },
      [`${componentCls}-actions-btn`]: {
        "&-disabled": {
          opacity: 0.45
        },
        "&-loading-button": {
          padding: 0,
          border: 0
        },
        "&-loading-icon": {
          height: token.controlHeight,
          width: token.controlHeight,
          verticalAlign: "top"
        },
        "&-recording-icon": {
          height: "1.2em",
          width: "1.2em",
          verticalAlign: "top"
        }
      },
      // ============================ Footer =============================
      [`${componentCls}-footer`]: {
        paddingInlineStart: padding,
        paddingInlineEnd: paddingSM,
        paddingBlockEnd: paddingSM,
        paddingBlockStart: paddingXXS,
        boxSizing: "border-box"
      }
    }
  };
};
var prepareComponentToken = () => ({});
var style_default = genStyleHooks("Sender", (token) => {
  const {
    paddingXS,
    calc
  } = token;
  const SenderToken = merge(token, {
    SenderContentMaxWidth: `calc(100% - ${unit(calc(paddingXS).add(32).equal())})`
  });
  return [genSenderStyle(SenderToken), header_default(SenderToken)];
}, prepareComponentToken);

// node_modules/@ant-design/x/es/sender/useSpeech.js
var import_react4 = __toESM(require_react());
var SpeechRecognition;
if (!SpeechRecognition && typeof window !== "undefined") {
  SpeechRecognition = window.SpeechRecognition || window.webkitSpeechRecognition;
}
function useSpeech(onSpeech, allowSpeech) {
  const onEventSpeech = useEvent(onSpeech);
  const [controlledRecording, onControlledRecordingChange, speechInControlled] = import_react4.default.useMemo(() => {
    if (typeof allowSpeech === "object") {
      return [allowSpeech.recording, allowSpeech.onRecordingChange, typeof allowSpeech.recording === "boolean"];
    }
    return [void 0, void 0, false];
  }, [allowSpeech]);
  const [permissionState, setPermissionState] = import_react4.default.useState(null);
  import_react4.default.useEffect(() => {
    if (typeof navigator !== "undefined" && "permissions" in navigator) {
      let lastPermission = null;
      navigator.permissions.query({
        name: "microphone"
      }).then((permissionStatus) => {
        setPermissionState(permissionStatus.state);
        permissionStatus.onchange = function() {
          setPermissionState(this.state);
        };
        lastPermission = permissionStatus;
      });
      return () => {
        if (lastPermission) {
          lastPermission.onchange = null;
        }
      };
    }
  }, []);
  const mergedAllowSpeech = SpeechRecognition && permissionState !== "denied";
  const recognitionRef = import_react4.default.useRef(null);
  const [recording, setRecording] = useMergedState(false, {
    value: controlledRecording
  });
  const forceBreakRef = import_react4.default.useRef(false);
  const ensureRecognition = () => {
    if (mergedAllowSpeech && !recognitionRef.current) {
      const recognition = new SpeechRecognition();
      recognition.onstart = () => {
        setRecording(true);
      };
      recognition.onend = () => {
        setRecording(false);
      };
      recognition.onresult = (event) => {
        var _a, _b, _c;
        if (!forceBreakRef.current) {
          const transcript = (_c = (_b = (_a = event.results) == null ? void 0 : _a[0]) == null ? void 0 : _b[0]) == null ? void 0 : _c.transcript;
          onEventSpeech(transcript);
        }
        forceBreakRef.current = false;
      };
      recognitionRef.current = recognition;
    }
  };
  const triggerSpeech = useEvent((forceBreak) => {
    if (forceBreak && !recording) {
      return;
    }
    forceBreakRef.current = forceBreak;
    if (speechInControlled) {
      onControlledRecordingChange == null ? void 0 : onControlledRecordingChange(!recording);
    } else {
      ensureRecognition();
      if (recognitionRef.current) {
        if (recording) {
          recognitionRef.current.stop();
          onControlledRecordingChange == null ? void 0 : onControlledRecordingChange(false);
        } else {
          recognitionRef.current.start();
          onControlledRecordingChange == null ? void 0 : onControlledRecordingChange(true);
        }
      }
    }
  });
  return [mergedAllowSpeech, triggerSpeech, recording];
}

// node_modules/@ant-design/x/es/sender/index.js
function getComponent(components, path, defaultComponent) {
  return get(components, path) || defaultComponent;
}
var sharedRenderComponents = {
  SendButton: SendButton_default,
  ClearButton: ClearButton_default,
  LoadingButton: LoadingButton_default,
  SpeechButton: SpeechButton_default
};
var ForwardSender = import_react5.default.forwardRef((props, ref) => {
  const {
    prefixCls: customizePrefixCls,
    styles = {},
    classNames: classNames4 = {},
    className,
    rootClassName,
    style,
    defaultValue,
    value,
    readOnly,
    submitType = "enter",
    onSubmit,
    loading,
    components,
    onCancel,
    onChange,
    actions,
    onKeyPress,
    onKeyDown,
    disabled,
    allowSpeech,
    prefix,
    footer,
    header,
    onPaste,
    onPasteFile,
    autoSize = {
      maxRows: 8
    },
    ...rest
  } = props;
  const {
    direction,
    getPrefixCls
  } = use_x_provider_context_default();
  const prefixCls = getPrefixCls("sender", customizePrefixCls);
  const containerRef = import_react5.default.useRef(null);
  const inputRef = import_react5.default.useRef(null);
  useProxyImperativeHandle(ref, () => {
    var _a, _b;
    return {
      nativeElement: containerRef.current,
      focus: (_a = inputRef.current) == null ? void 0 : _a.focus,
      blur: (_b = inputRef.current) == null ? void 0 : _b.blur
    };
  });
  const contextConfig = use_x_component_config_default("sender");
  const inputCls = `${prefixCls}-input`;
  const [wrapCSSVar, hashId, cssVarCls] = style_default(prefixCls);
  const mergedCls = (0, import_classnames4.default)(prefixCls, contextConfig.className, className, rootClassName, hashId, cssVarCls, {
    [`${prefixCls}-rtl`]: direction === "rtl",
    [`${prefixCls}-disabled`]: disabled
  });
  const actionBtnCls = `${prefixCls}-actions-btn`;
  const actionListCls = `${prefixCls}-actions-list`;
  const [innerValue, setInnerValue] = useMergedState(defaultValue || "", {
    value
  });
  const triggerValueChange = (nextValue, event) => {
    setInnerValue(nextValue);
    if (onChange) {
      onChange(nextValue, event);
    }
  };
  const [speechPermission, triggerSpeech, speechRecording] = useSpeech((transcript) => {
    triggerValueChange(`${innerValue} ${transcript}`);
  }, allowSpeech);
  const InputTextArea = getComponent(components, ["input"], input_default.TextArea);
  const domProps = pickAttrs(rest, {
    attr: true,
    aria: true,
    data: true
  });
  const inputProps = {
    ...domProps,
    ref: inputRef
  };
  const triggerSend = () => {
    if (innerValue && onSubmit && !loading) {
      onSubmit(innerValue);
    }
  };
  const triggerClear = () => {
    triggerValueChange("");
  };
  const isCompositionRef = import_react5.default.useRef(false);
  const onInternalCompositionStart = () => {
    isCompositionRef.current = true;
  };
  const onInternalCompositionEnd = () => {
    isCompositionRef.current = false;
  };
  const onInternalKeyPress = (e) => {
    const canSubmit = e.key === "Enter" && !isCompositionRef.current;
    switch (submitType) {
      case "enter":
        if (canSubmit && !e.shiftKey) {
          e.preventDefault();
          triggerSend();
        }
        break;
      case "shiftEnter":
        if (canSubmit && e.shiftKey) {
          e.preventDefault();
          triggerSend();
        }
        break;
    }
    onKeyPress == null ? void 0 : onKeyPress(e);
  };
  const onInternalPaste = (e) => {
    var _a;
    const files = (_a = e.clipboardData) == null ? void 0 : _a.files;
    if ((files == null ? void 0 : files.length) && onPasteFile) {
      onPasteFile(files[0], files);
      e.preventDefault();
    }
    onPaste == null ? void 0 : onPaste(e);
  };
  const onContentMouseDown = (e) => {
    var _a, _b;
    if (e.target !== ((_a = containerRef.current) == null ? void 0 : _a.querySelector(`.${inputCls}`))) {
      e.preventDefault();
    }
    (_b = inputRef.current) == null ? void 0 : _b.focus();
  };
  let actionNode = import_react5.default.createElement(flex_default, {
    className: `${actionListCls}-presets`
  }, allowSpeech && import_react5.default.createElement(SpeechButton_default, null), loading ? import_react5.default.createElement(LoadingButton_default, null) : import_react5.default.createElement(SendButton_default, null));
  if (typeof actions === "function") {
    actionNode = actions(actionNode, {
      components: sharedRenderComponents
    });
  } else if (actions || actions === false) {
    actionNode = actions;
  }
  const actionsButtonContextProps = {
    prefixCls: actionBtnCls,
    onSend: triggerSend,
    onSendDisabled: !innerValue,
    onClear: triggerClear,
    onClearDisabled: !innerValue,
    onCancel,
    onCancelDisabled: !loading,
    onSpeech: () => triggerSpeech(false),
    onSpeechDisabled: !speechPermission,
    speechRecording,
    disabled
  };
  const footerNode = typeof footer === "function" ? footer({
    components: sharedRenderComponents
  }) : footer || null;
  return wrapCSSVar(import_react5.default.createElement("div", {
    ref: containerRef,
    className: mergedCls,
    style: {
      ...contextConfig.style,
      ...style
    }
  }, header && import_react5.default.createElement(SendHeaderContext.Provider, {
    value: {
      prefixCls
    }
  }, header), import_react5.default.createElement(ActionButtonContext.Provider, {
    value: actionsButtonContextProps
  }, import_react5.default.createElement("div", {
    className: (0, import_classnames4.default)(`${prefixCls}-content`, contextConfig.classNames.content, classNames4.content),
    style: {
      ...contextConfig.styles.content,
      ...styles.content
    },
    onMouseDown: onContentMouseDown
  }, prefix && import_react5.default.createElement("div", {
    className: (0, import_classnames4.default)(`${prefixCls}-prefix`, contextConfig.classNames.prefix, classNames4.prefix),
    style: {
      ...contextConfig.styles.prefix,
      ...styles.prefix
    }
  }, prefix), import_react5.default.createElement(InputTextArea, _extends({}, inputProps, {
    disabled,
    style: {
      ...contextConfig.styles.input,
      ...styles.input
    },
    className: (0, import_classnames4.default)(inputCls, contextConfig.classNames.input, classNames4.input),
    autoSize,
    value: innerValue,
    onChange: (event) => {
      triggerValueChange(event.target.value, event);
      triggerSpeech(true);
    },
    onPressEnter: onInternalKeyPress,
    onCompositionStart: onInternalCompositionStart,
    onCompositionEnd: onInternalCompositionEnd,
    onKeyDown,
    onPaste: onInternalPaste,
    variant: "borderless",
    readOnly
  })), actionNode && import_react5.default.createElement("div", {
    className: (0, import_classnames4.default)(actionListCls, contextConfig.classNames.actions, classNames4.actions),
    style: {
      ...contextConfig.styles.actions,
      ...styles.actions
    }
  }, actionNode)), footerNode && import_react5.default.createElement("div", {
    className: (0, import_classnames4.default)(`${prefixCls}-footer`, contextConfig.classNames.footer, classNames4.footer),
    style: {
      ...contextConfig.styles.footer,
      ...styles.footer
    }
  }, footerNode))));
});
var Sender = ForwardSender;
if (true) {
  Sender.displayName = "Sender";
}
Sender.Header = SenderHeader;
var sender_default = Sender;
export {
  sender_default as default
};
//# sourceMappingURL=@ant-design_x_es_sender.js.map
